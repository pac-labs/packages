# Zed binary

This source builds the PAC executable used by Zed as a local context server.

Build it from **Sources → binaries → zed-binary**. The build action compiles current source for supported OS and architecture targets.

The resulting files appear in the binary artifacts panel.
